# Arquivo .env para Desenvolvimento Local
# Copie este arquivo e renomeie para .env

# Ambiente (development, testing, production)
FLASK_ENV=development
FLASK_CONFIG=development

# Chave secreta (mude em produção!)
SECRET_KEY=dev-secret-key-change-in-production

# Database
DEV_DATABASE_URL=sqlite:///kaizen_dev.db

# CORS Origins (separados por vírgula)
# Para desenvolvimento local, use * para aceitar qualquer origem
CORS_ORIGINS=*

# JWT
JWT_SECRET_KEY=dev-jwt-secret-key

# Flask
FLASK_APP=run.py
FLASK_DEBUG=1
